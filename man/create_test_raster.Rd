% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generators.R
\name{create_test_raster}
\alias{create_test_raster}
\title{Create a test SpatRaster with known properties}
\usage{
create_test_raster(
  nrow = 10,
  ncol = 10,
  nlyr = 1,
  crs = "EPSG:4326",
  xmin = 0,
  xmax = 1,
  ymin = 0,
  ymax = 1,
  values = NULL,
  na_fraction = 0,
  seed = NULL
)
}
\arguments{
\item{nrow}{Integer. Number of rows. Default: 10}

\item{ncol}{Integer. Number of columns. Default: 10}

\item{nlyr}{Integer. Number of layers. Default: 1}

\item{crs}{Character. CRS string (EPSG code or WKT). Default: `"EPSG:4326"`}

\item{xmin, xmax, ymin, ymax}{Numeric. Extent of the raster}

\item{values}{Numeric vector or `NULL`. If `NULL` (default), cells are filled
with sequential values 1 to `nrow * ncol * nlyr`. If a single value is
supplied, all cells receive that value. Otherwise the vector is recycled}

\item{na_fraction}{Numeric in \[0, 1\]. Fraction of cells to set to `NA`
Default: 0 (no NAs)}

\item{seed}{Integer or `NULL`. Random seed for reproducible NA placement}
}
\value{
A `terra::SpatRaster` object
}
\description{
Generates a synthetic `terra` SpatRaster with controllable dimensions,
CRS, and cell values. Useful as reproducible input for unit tests of
raster-processing functions.
}
\examples{
# simple 10x10 raster with sequential values
r <- create_test_raster()

# UTM raster for tests that need concrete numeric values
r_utm <- create_test_raster(crs = "EPSG:32632",
                            xmin = 300000, xmax = 310000,
                            ymin = 5000000, ymax = 5010000)

# raster with 20\% NA cells, reproducible
r_na <- create_test_raster(na_fraction = 0.2, seed = 42)

}
