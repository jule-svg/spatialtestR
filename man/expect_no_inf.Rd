% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expect-robust.R
\name{expect_no_inf}
\alias{expect_no_inf}
\title{Assert that a SpatRaster contains no Inf or -Inf values}
\usage{
expect_no_inf(r)
}
\arguments{
\item{r}{A `terra::SpatRaster`.}
}
\value{
`r`, invisibly (for pipe use).
}
\description{
Fails if any non-NA cell in `r` is infinite, reporting the separate counts
of `Inf` and `-Inf` values found.
}
\examples{
# generators never produce Inf
r <- create_test_raster(values = 1:100)
expect_no_inf(r)

# arithmetic with finite values stays finite
expect_no_inf(r * 2)

}
